<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_close_stale_conversations" model="ir.cron">
            <field name="name">WhatsApp: Close Stale Conversations</field>
            <field name="model_id" ref="model_whatsapp_conversation"/>
            <field name="state">code</field>
            <field name="code">
stale = datetime.datetime.now() - datetime.timedelta(hours=24)
recs = env['whatsapp.conversation'].search([
    ('state', 'in', ['active', 'ai_handled']),
    ('last_message_date', '&lt;', stale),
])
recs.write({'state': 'closed'})
            </field>
            <field name="interval_number">6</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
            <field name="numbercall">-1</field>
        </record>
    </data>
</odoo>
